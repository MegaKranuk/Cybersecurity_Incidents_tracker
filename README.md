# Трекер інцидентів кібербезпеки

Вебзастосунок для реєстрації, моніторингу та управління кіберінцидентами.
Робота виконана в межах лабораторної роботи №1 (Варіант 2).

## Реалізований функціонал
- **Створення:** введення даних про інцидент з валідацією;
- **Редагування:** можливість зміни вже створених записів;
- **Видалення:** видалення неактуальних інцидентів;
- **Сортування та фільтрація:** сортування за датою, фільтр за типом та критичністю;
- **Збереження:** клієнт-серверна взаємодія (дані зберігаються через REST API на локальному бекенді);
- **Відображення:** динамічний рендер таблиці.


## Валідація введених даних
- дата інциденту — обовʼязкова, не пізніше поточної;
- тип інциденту — обовʼязкове поле;
- критичність — обовʼязкове поле;
- імʼя репортера — від 5 до 15 символів;
- опис інциденту — від 15 до 50 символів.

### Захист від XSS (Cross-Site Scripting)
Оскільки система призначена для реєстрації кіберінцидентів, користувачі можуть вводити у поля "Опис" або "Репортер" приклади шкідливого коду (payloads).

Щоб запобігти виконанню цього коду в браузері (XSS-атака), реалізовано механізм **Input Sanitization**.
* Перед рендерингом таблиці всі дані проходять через функцію-санітайзер.
* Спеціальні символи замінюються на безпечні HTML-сутності:
  * `<` замінюється на `&lt;`
  * `>` замінюється на `&gt;`
  * `&` замінюється на `&amp;`

**Результат:** Якщо ввести `<script>alert('Hacked')</script>`, застосунок відобразить це як безпечний текст, а не виконає скрипт.

## Використані технології
- **HTML5:** семантична розмітка;
- **CSS3:** Flexbox, змінні, адаптивність;
- **JavaScript:** DOM API, Event Delegation, Fetch API;
- **Backend:** Node.js, Express, TypeScript (обробка API запитів та генерація UUID).

## Запуск проєкту
1. Склонуйте репозиторій.
2. **Запуск сервера:** перейдіть у папку бекенду, виконайте `npm install`, а потім `npm run dev` (API запуститься на порту 3000).
3. **Запуск клієнта:** відкрийте файл `index.html` у браузері (або використовуйте розширення Live Server у VS Code).

## 🧪 Сценарії тестування (curl-команди)

Для перевірки якості роботи API згідно з пунктом 4 методичних вказівок, виконайте наступні команди в терміналі:

1. **Перевірка отримання списку (GET):**
   `curl -X GET "http://localhost:3000/api/incidents"`

2. **Створення нового інциденту (POST):**
   `curl -X POST "http://localhost:3000/api/incidents" -H "Content-Type: application/json" -d "{\"date\":\"2026-03-03\",\"tag\":\"Фішинг\",\"criticality\":\"Середня критичність\",\"reporter\":\"Tester\",\"comment\":\"Тестовий опис інциденту довжиною понад 15 символів\"}"`

3. **Перевірка фільтрації (Query Param):**
   `curl -X GET "http://localhost:3000/api/incidents?tag=Фішинг"`

4. **Перевірка валідації (очікувана помилка 400):**
   `curl -i -X POST "http://localhost:3000/api/incidents" -H "Content-Type: application/json" -d "{\"reporter\":\"Short\"}"`

## Версія
Фінальна версія лабораторної роботи позначена тегом **0.2.0**.

